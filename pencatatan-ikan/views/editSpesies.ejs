<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Spesies</title>
    <link
      rel="stylesheet"
      href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />
  </head>
  <body class="d-flex flex-column min-vh-100">
    <%- include('header') %>
    <div class="container mt-5">
      <h1>Edit Spesies</h1>
      <form action="/spesies/update/<%= spesies.id %>" method="POST">
        <div class="form-group">
          <label for="nama_spesies">Nama Spesies</label>
          <input
            type="text"
            class="form-control"
            name="nama_spesies"
            id="nama_spesies"
            value="<%= spesies.nama_spesies %>"
            required
          />
        </div>
        <button class="btn btn-success" type="submit">Simpan Perubahan</button>
        <a href="/spesies" class="btn btn-secondary">Batal</a>
      </form>
    </div>
    <%- include('footer') %>
  </body>
</html>
